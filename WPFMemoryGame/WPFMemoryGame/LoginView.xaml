<UserControl x:Class="WPFMemoryGame.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFMemoryGame"
        mc:Ignorable="d" Height="450" Width="800">
    <Grid Background="Gray">
        <Grid.RowDefinitions>
            <!-- Welcome text-->
            <RowDefinition Height="Auto"/>
            <!-- Users panel + user image/selector -->
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <!-- Welcome text-->
        <TextBlock Grid.Row="1" Text="Welcome to MemoryGame!!!" FontSize="24" FontWeight="Medium" Foreground="DarkOliveGreen" HorizontalAlignment="Center" Padding="14"/>
        <!-- Users panel + user image/selector -->
        <Grid Grid.Row="2" Background="Gray">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <!-- User List-->
            <ListBox ItemsSource="{Binding UserList}" SelectedItem="{Binding SelectedUser, Mode=TwoWay}" Grid.Row="0" Background="Gray">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal" Background="Gray">
                            <Button Content="{Binding}" VerticalAlignment="Center" Width="Auto" Command="{Binding DataContext.Users, RelativeSource={RelativeSource AncestorType=Window}}"
                        CommandParameter="{Binding}" />
                        </StackPanel>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>

        </Grid>

    </Grid>
</UserControl>
